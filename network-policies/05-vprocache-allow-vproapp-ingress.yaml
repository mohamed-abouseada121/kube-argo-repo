apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vprocache-allow-vproapp-ingress
  namespace: vprofile
spec:
  podSelector:
    matchLabels:
      app: vprocache
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: vproapp
    ports:
    - protocol: TCP
      port: 11211